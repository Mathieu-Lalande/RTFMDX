{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Lancer l'extension MXT",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": ["${workspaceFolder}/**/*.js"],
      "preLaunchTask": "npm: postinstall"
    }
  ]
}
